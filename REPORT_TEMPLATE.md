# Алгоритмын шинжилгээ ба зохиомж

## Бие даалт №1: Граф хайлтын алгоритмууд

**Оюутны нэр:** [Таны нэр]
**Оюутны код:** [Таны код]
**Хичээлийн нэр:** Алгоритмын шинжилгээ ба зохиомж
**Огноо:** [Огноо]

---

## 1. Оршил

### 1.1 Төслийн зорилго

Энэхүү төсөл нь OpenStreetMap-с татсан жинхэнэ замын өгөгдлийг ашиглан граф хайлтын алгоритмуудыг (BFS, DFS, Dijkstra) судлах, хэрэгжүүлэх, харьцуулах зорилготой юм.

### 1.2 Ашигласан технологи

-  **Хэл:** Python 3.8+
-  **Граф сан:** NetworkX
-  **Газарзүйн өгөгдөл:** GeoPandas
-  **Backend:** Flask REST API
-  **Frontend:** HTML5, CSS3, JavaScript, Leaflet.js
-  **Өгөгдөл:** OpenStreetMap (gis_osm_roads_free_1.shp)

### 1.3 Өгөгдлийн тайлбар

OpenStreetMap (OSM) нь чөлөөтэй ашиглах боломжтой газрын зургийн өгөгдлийн санд юм. Бид Shapefile форматаар замын сүлжээг авч, түүнийг граф болгон хувиргасан.

**Графын бүтэц:**

-  **Оройнууд (Vertices):** Замын уулзвар, эхлэл, төгсгөлийн цэгүүд (координатууд)
-  **Ирмэгүүд (Edges):** Замууд (хоёр цэгийн хоорондох холбоос)
-  **Жин (Weight):** Замын урт (км-ээр Haversine томъёогоор тооцсон)

---

## 2. BFS (Breadth-First Search) Алгоритм

### 2.1 Онолын үндэс

**Breadth-First Search (Өргөнөөр эхлэн хайх)** нь графыг хайх үндсэн алгоритм юм. Энэ нь эхлэх оройгоос эхлээд бүх хөрш оройнуудыг түрүүлж үзэж, дараа нь тэдгээрийн хөршүүдийг үзнэ.

**Онцлог:**

-  Дараалал (Queue) ашигладаг
-  Жинг харгалзахгүй
-  Хамгийн цөөн оройгоор зам олно
-  Жин тэнцүү үед хамгийн богино зам олно

### 2.2 Хэрэгжүүлэлт

```python
def bfs(self, start_node, end_node):
    """BFS алгоритм"""
    from collections import deque

    queue = deque([(start_node, [start_node])])
    visited = set()
    visited_order = []

    while queue:
        current_node, path = queue.popleft()

        if current_node in visited:
            continue

        visited.add(current_node)
        visited_order.append(current_node)

        if current_node == end_node:
            # Замын нийт зайг тооцох
            total_distance = 0
            for i in range(len(path) - 1):
                edge_data = self.graph.get_edge_data(path[i], path[i+1])
                total_distance += edge_data.get('weight', 0)

            return {
                'path': path,
                'visited': visited_order,
                'distance': total_distance,
                'found': True
            }

        # Хөрш оройнуудыг дараалалд нэмэх
        for neighbor in self.graph.neighbors(current_node):
            if neighbor not in visited:
                queue.append((neighbor, path + [neighbor]))

    return {'path': [], 'visited': visited_order, 'distance': 0, 'found': False}
```

### 2.3 Нарийн төвөгт (Complexity)

-  **Хугацааны нарийн төвөгт:** O(V + E)

   -  V: Оройн тоо
   -  E: Ирмэгийн тоо
   -  Бүх орой болон ирмэгийг нэг л удаа үздэг

-  **Санах ойн нарийн төвөгт:** O(V)
   -  Дараалал болон visited багцад хамгийн ихдээ V орой хадгалагдана

### 2.4 Үр дүн

**Жишээ тест:**

-  Эхлэх цэг: (47.9186, 106.9177)
-  Төгсгөх цэг: (47.9286, 106.9277)
-  Замын урт: [X] орой
-  Нийт зай: [Y] км
-  Хайсан орой: [Z] орой
-  Гүйцэтгэх хугацаа: [T] мс

**Давуу тал:**

-  Энгийн хэрэгжүүлэлт
-  Цөөн оройтой зам олно
-  Системчлэлтэй хайлт

**Сул тал:**

-  Жинг харгалзахгүй
-  Жинтэй графд оновчгүй
-  Их санах ой шаарддаг (том граф дээр)

---

## 3. DFS (Depth-First Search) Алгоритм

### 3.1 Онолын үндэс

**Depth-First Search (Гүнээр эхлэн хайх)** нь графыг гүн рүү эхлээд хайдаг алгоритм юм. Нэг чиглэлд боломжит хамгийн холыг үзэж, буцаад өөр чиглэл рүү ордог.

**Онцлог:**

-  Стек (Stack) ашигладаг
-  Рекурсив эсвэл итератив хэрэгжүүлэх боломжтой
-  Гүн рүү түрүүлж хайна
-  Зам олох баталгаа өгдөг (холбогдсон графд)

### 3.2 Хэрэгжүүлэлт

```python
def dfs(self, start_node, end_node):
    """DFS алгоритм"""
    stack = [(start_node, [start_node])]
    visited = set()
    visited_order = []

    while stack:
        current_node, path = stack.pop()

        if current_node in visited:
            continue

        visited.add(current_node)
        visited_order.append(current_node)

        if current_node == end_node:
            # Замын нийт зайг тооцох
            total_distance = 0
            for i in range(len(path) - 1):
                edge_data = self.graph.get_edge_data(path[i], path[i+1])
                total_distance += edge_data.get('weight', 0)

            return {
                'path': path,
                'visited': visited_order,
                'distance': total_distance,
                'found': True
            }

        # Хөрш оройнуудыг стэк рүү нэмэх
        neighbors = list(self.graph.neighbors(current_node))
        for neighbor in reversed(neighbors):
            if neighbor not in visited:
                stack.append((neighbor, path + [neighbor]))

    return {'path': [], 'visited': visited_order, 'distance': 0, 'found': False}
```

### 3.3 Нарийн төвөгт (Complexity)

-  **Хугацааны нарийн төвөгт:** O(V + E)

   -  BFS-тэй ижил
   -  Бүх орой болон ирмэгийг үзнэ

-  **Санах ойн нарийн төвөгт:** O(V)
   -  Муу тохиолдолд стекийн гүн V хүрч болно
   -  Рекурсив хувилбарт call stack ашигладаг

### 3.4 Үр дүн

**Жишээ тест:**

-  Эхлэх цэг: (47.9186, 106.9177)
-  Төгсгөх цэг: (47.9286, 106.9277)
-  Замын урт: [X] орой
-  Нийт зай: [Y] км
-  Хайсан орой: [Z] орой
-  Гүйцэтгэх хугацаа: [T] мс

**Давуу тал:**

-  Энгийн хэрэгжүүлэлт
-  Цикл илрүүлэхэд тохиромжтой
-  Санах ой хэмнэлттэй (зарим тохиолдолд)

**Сул тал:**

-  Хамгийн богино зам олохгүй
-  Жинг харгалзахгүй
-  Мухар гүнд орж болно

---

## 4. Dijkstra Алгоритм

### 4.1 Онолын үндэс

**Dijkstra алгоритм** нь Эдсгер Дэйкстра 1956 онд зохиосон, жинтэй графт хамгийн богино замыг олох алгоритм юм. Энэ нь greedy алгоритмын нэг төрөл бөгөөд сөрөг жингүй графт оновчтой ажилладаг.

**Онцлог:**

-  Priority Queue (Давуу эрэмбийн дараалал) ашигладаг
-  Жинг харгалзана
-  Хамгийн богино зам олох баталгаатай
-  Сөрөг жинтэй ирмэг байж болохгүй

### 4.2 Хэрэгжүүлэлт

```python
def dijkstra(self, start_node, end_node):
    """Dijkstra алгоритм"""
    import heapq

    # Priority queue: (зай, орой, зам)
    pq = [(0, start_node, [start_node])]
    distances = {start_node: 0}
    visited = set()
    visited_order = []

    while pq:
        current_dist, current_node, path = heapq.heappop(pq)

        if current_node in visited:
            continue

        visited.add(current_node)
        visited_order.append(current_node)

        if current_node == end_node:
            return {
                'path': path,
                'visited': visited_order,
                'distance': current_dist,
                'found': True
            }

        # Хөрш оройнуудыг шалгах
        for neighbor in self.graph.neighbors(current_node):
            if neighbor not in visited:
                edge_data = self.graph.get_edge_data(current_node, neighbor)
                new_dist = current_dist + edge_data.get('weight', 0)

                if neighbor not in distances or new_dist < distances[neighbor]:
                    distances[neighbor] = new_dist
                    heapq.heappush(pq, (new_dist, neighbor, path + [neighbor]))

    return {'path': [], 'visited': visited_order, 'distance': 0, 'found': False}
```

### 4.3 Нарийн төвөгт (Complexity)

-  **Хугацааны нарийн төвөгт:** O((V + E) log V)

   -  Binary heap ашигласан тохиолдолд
   -  Fibonacci heap ашигласан тохиолдолд O(E + V log V)
   -  Priority queue-ын операциуд log V нарийн төвөгттэй

-  **Санах ойн нарийн төвөгт:** O(V)
   -  Priority queue, distances dictionary
   -  visited set

### 4.4 Үр дүн

**Жишээ тест:**

-  Эхлэх цэг: (47.9186, 106.9177)
-  Төгсгөх цэг: (47.9286, 106.9277)
-  Замын урт: [X] орой
-  Нийт зай: [Y] км (хамгийн богино!)
-  Хайсан орой: [Z] орой
-  Гүйцэтгэх хугацаа: [T] мс

**Давуу тал:**

-  Хамгийн богино зам олох баталгаатай
-  Жинтэй графт оновчтой
-  Практикт өргөн хэрэглэгддэг (GPS навигаци гэх мэт)

**Сул тал:**

-  BFS, DFS-с илүү нарийн төвөгттэй
-  Сөрөг жинтэй графд ажиллахгүй
-  Илүү их санах ой шаарддаг

---

## 5. Алгоритмуудын харьцуулалт

### 5.1 Харьцуулах хүснэгт

| Шинж чанар              | BFS        | DFS    | Dijkstra       |
| ----------------------- | ---------- | ------ | -------------- |
| Хугацааны нарийн төвөгт | O(V+E)     | O(V+E) | O((V+E) log V) |
| Санах ойн нарийн төвөгт | O(V)       | O(V)   | O(V)           |
| Жин харгалзах           | Үгүй       | Үгүй   | Тийм           |
| Хамгийн богино зам      | Жингүй үед | Үгүй   | Тийм           |
| Өгөгдлийн бүтэц         | Queue      | Stack  | Priority Queue |
| Хайлтын стратеги        | Өргөн      | Гүн    | Greedy         |

### 5.2 Туршилтын үр дүн

**Тест орчин:**

-  Операцион систем: [ОС]
-  Процессор: [CPU]
-  RAM: [RAM хэмжээ]
-  Python хувилбар: [Python version]

**Туршилтын өгөгдөл:**

| Тест | Эхлэх               | Төгсгөх             | Орой | Ирмэг |
| ---- | ------------------- | ------------------- | ---- | ----- |
| 1    | (47.9186, 106.9177) | (47.9286, 106.9277) | [V1] | [E1]  |
| 2    | [lat2, lon2]        | [lat2, lon2]        | [V2] | [E2]  |
| 3    | [lat3, lon3]        | [lat3, lon3]        | [V3] | [E3]  |

**Үр дүн:**

| Тест | Алгоритм | Зай (км) | Замын урт | Хайсан орой | Хугацаа (мс) |
| ---- | -------- | -------- | --------- | ----------- | ------------ |
| 1    | BFS      | [d1]     | [p1]      | [v1]        | [t1]         |
| 1    | DFS      | [d1]     | [p1]      | [v1]        | [t1]         |
| 1    | Dijkstra | [d1]     | [p1]      | [v1]        | [t1]         |
| 2    | BFS      | [d2]     | [p2]      | [v2]        | [t2]         |
| 2    | DFS      | [d2]     | [p2]      | [v2]        | [t2]         |
| 2    | Dijkstra | [d2]     | [p2]      | [v2]        | [t2]         |

### 5.3 Шинжилгээ

**Гүйцэтгэлийн үнэлгээ:**

1. **Хамгийн богино зам:**

   -  Dijkstra алгоритм үргэлж хамгийн богино замыг олсон
   -  BFS, DFS нь илүү урт зам олсон

2. **Гүйцэтгэх хугацаа:**

   -  [Дүн шинжилгээ оруулах]
   -  Жижиг графд 3-ууланг нь хурдан
   -  Том графд Dijkstra удаашралтай

3. **Санах ойн хэрэглээ:**

   -  [Дүн шинжилгээ оруулах]
   -  Бүгд O(V) нарийн төвөгттэй

4. **Хайсан оройн тоо:**
   -  BFS: Системчлэлтэй, тогтмол
   -  DFS: Хувьсах, тодорхойгүй
   -  Dijkstra: Оновчтой

### 5.4 График дүрслэл

[Энд график зураг оруулах - matplotlib ашиглаж болно]

```python
import matplotlib.pyplot as plt

# Жишээ код:
algorithms = ['BFS', 'DFS', 'Dijkstra']
distances = [12.5, 15.3, 10.8]
times = [45, 38, 52]

fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(12, 5))

ax1.bar(algorithms, distances)
ax1.set_ylabel('Зай (км)')
ax1.set_title('Олдсон замын урт')

ax2.bar(algorithms, times)
ax2.set_ylabel('Хугацаа (мс)')
ax2.set_title('Гүйцэтгэх хугацаа')

plt.tight_layout()
plt.savefig('comparison.png')
```

---

## 6. Системийн архитектур

### 6.1 Backend (Flask REST API)

**Үндсэн endpoints:**

-  `GET /api/info` - Графын мэдээлэл
-  `POST /api/search` - Зам хайх
-  `POST /api/compare` - Харьцуулах
-  `POST /api/visualize` - Визуалчлах

### 6.2 Frontend

**Технологи:**

-  HTML5, CSS3 - Бүтэц, загвар
-  JavaScript - Интерактив функционал
-  Leaflet.js - Газрын зургийн визуализаци

**Онцлогууд:**

-  Интерактив газрын зураг
-  Координат сонгох (дарах)
-  Алгоритм сонгох
-  Үр дүн харуулах
-  Харьцуулалтын хүснэгт

### 6.3 Визуализаци

**Folium сан:**

-  Python дээр интерактив газрын зураг үүсгэнэ
-  Leaflet.js дээр суурилсан
-  HTML файл үүсгэж хөтөч дээр харуулна

**Визуализацийн элементүүд:**

-  🟢 Эхлэх цэг (ногоон)
-  🔴 Төгсгөх цэг (улаан)
-  🟠 Хайсан оройнууд (улбар шар цэгүүд)
-  🔵 Олдсон зам (цэнхэр шугам)

---

## 7. Кодын жишээ

### 7.1 Граф үүсгэх

```python
from read_osm import RoadNetworkGraph

# Shapefile унших
road_network = RoadNetworkGraph("gis_osm_roads_free_1.shp")
G = road_network.get_graph()

print(f"Оройн тоо: {G.number_of_nodes()}")
print(f"Ирмэгийн тоо: {G.number_of_edges()}")
```

### 7.2 Алгоритм ажиллуулах

```python
# Хамгийн ойр оройнуудыг олох
start = road_network.find_nearest_node(47.9186, 106.9177)
end = road_network.find_nearest_node(47.9286, 106.9277)

# BFS
bfs_result = road_network.bfs(start, end)
print(f"BFS зай: {bfs_result['distance']:.2f} км")

# DFS
dfs_result = road_network.dfs(start, end)
print(f"DFS зай: {dfs_result['distance']:.2f} км")

# Dijkstra
dijkstra_result = road_network.dijkstra(start, end)
print(f"Dijkstra зай: {dijkstra_result['distance']:.2f} км")
```

### 7.3 API ашиглах

```bash
# cURL жишээ
curl -X POST http://localhost:5000/api/search \
  -H "Content-Type: application/json" \
  -d '{
    "algorithm": "dijkstra",
    "start_lat": 47.9186,
    "start_lon": 106.9177,
    "end_lat": 47.9286,
    "end_lon": 106.9277
  }'
```

---

## 8. Дүгнэлт

### 8.1 Хийсэн ажлын дүгнэлт

Энэхүү төсөлд бид:

1. ✅ OpenStreetMap өгөгдлөөс граф үүсгэсэн
2. ✅ BFS, DFS, Dijkstra алгоритмуудыг хэрэгжүүлсэн
3. ✅ Flask REST API backend бүтээсэн
4. ✅ Сайхан веб интерфейс хөгжүүлсэн
5. ✅ Газрын зураг дээр визуализаци хийсэн
6. ✅ Алгоритмуудыг харьцуулсан

### 8.2 Сурсан зүйлс

**Онолын хувьд:**

-  Граф хайлтын алгоритмуудын үндэс
-  Нарийн төвөгтийн шинжилгээ
-  Алгоритм сонгох аргачлал

**Практикийн хувьд:**

-  Python граф сангууд (NetworkX, GeoPandas)
-  REST API бүтээх (Flask)
-  Веб визуализаци (Leaflet.js, Folium)
-  Жинхэнэ өгөгдөл боловсруулах

### 8.3 Алгоритм сонгох зөвлөмж

**BFS ашиглах:**

-  Жингүй граф
-  Хамгийн цөөн алхам хайх
-  Тархалтын загвар судлах

**DFS ашиглах:**

-  Холбогдсон эсэхийг шалгах
-  Цикл илрүүлэх
-  Topology эрэмбэлэх

**Dijkstra ашиглах:**

-  Жинтэй граф
-  Хамгийн богино зам хайх
-  GPS навигаци, сүлжээний routing

### 8.4 Цаашдын хөгжүүлэлт

Төслийг сайжруулах боломжууд:

1. **A\* алгоритм** нэмэх (heuristic ашигладаг)
2. **Bellman-Ford** нэмэх (сөрөг жин дэмждэг)
3. **Олон цэгийн зам** (multiple waypoints)
4. **Жинхэнэ цагийн замын хөдөлгөөн** оруулах
5. **Mobile апп** бүтээх
6. **Өгөгдлийн сангийн интеграци** (PostgreSQL/PostGIS)

---

## 9. Ашигласан материал

### 9.1 Ном, эх сурвалж

1. Thomas H. Cormen et al., "Introduction to Algorithms" (CLRS), 3rd Edition
2. Steven Skiena, "The Algorithm Design Manual", 2nd Edition
3. NetworkX Documentation: https://networkx.org/
4. GeoPandas Documentation: https://geopandas.org/

### 9.2 Вэб эх үүсвэр

1. OpenStreetMap: https://www.openstreetmap.org/
2. Geofabrik Downloads: https://download.geofabrik.de/
3. Flask Documentation: https://flask.palletsprojects.com/
4. Leaflet.js: https://leafletjs.com/

### 9.3 Код репозитори

-  GitHub: [таны репозитори холбоос]

---

## Хавсралт

### А. Суулгах заавар

[README.md-с харна уу]

### Б. API баримт бичиг

[Дэлгэрэнгүй API specs]

### В. Скриншот

[Апп-н скриншотууд оруулах]

---

**Огноо:** [YYYY-MM-DD]
**Гарын үсэг:** **\*\***\_\_\_**\*\***
